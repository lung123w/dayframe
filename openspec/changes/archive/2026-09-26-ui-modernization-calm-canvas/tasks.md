# Tasks — ui-modernization-calm-canvas

Delivery rule: **one stage = one branch = one PR = one build card + one `df-tester` verification card**, chained with `parents`. A stage is not Done until its verification card passes — a tester pass **is** the acceptance gate (POL-001), so no card waits for a human. Every card body carries the must-not-regress guard.

Sanity check before any stage: `grep -c "ADR-012" .dev_context/DECISION_LOG.md` must not be 0 (the base is `design/ui-direction` and later the stage stack, never `master`).
Merge order: `master` ← PR #15 → PR #17 → PR #20 (`design/ui-direction`) → `design/ui-modernization` (this change) → `feat/ui-s0-dead-code` → `feat/ui-s1-tokens` → `feat/ui-s2-shell` → `feat/ui-s3-today-habits` → `feat/ui-s4-week-review-finance` → `feat/ui-s5-keyboard` → `docs/ui-modernization-archive`.

## 0. Baseline + dead code

- [x] 0.1 Record the scoped baseline **before** any deletion: `npx vitest run --exclude='**/.worktrees/**'` → 28 files / 331 tests / 1 pre-existing `MiniWeekBar.test.jsx:38-39` failure; `npx eslint src` → 8; `npx vite build` → ok. Also record the trap: unscoped runs collect nested `.worktrees/` copies (111 files / 1,309 tests / 6 failed) and `eslint .` reports 473. Write the numbers into `design.md` §10. (card `t_aa4715eb`, `df-fullstack`, branch `feat/ui-s0-dead-code` from `design/ui-modernization`) **Measured 2026-09-24 in this card's worktree of the tracked source:** scoped `npx vitest run --exclude='**/.worktrees/**'` = 27 files / 326 tests / 1 failed (`MiniWeekBar.test.jsx:38-39`, unchanged by the deletions), `npx eslint src` = 8, `npx vite build` = ok. That is one file / five tests below the 28 / 331 above only because the main working tree also carries the untracked `src/__tests__/wrModeStorage.test.jsx` (POL-003). The trap, re-measured from the repo root: 138 files / 1,635 tests / 5 failed and `eslint .` = 669 — collection artefacts of the nested `.worktrees/` checkouts, not regressions.
- [x] 0.2 Delete the 1,828 dead CSS lines and the four components that own them — `Calendar.jsx`/`.css` (916), `DayPanel.jsx`/`.css` (486), `OutstandingTasks.jsx`/`.css` (353), `DailyShutdown.jsx`/`.css` (73) — and the dangling `vi.mock('../components/DailyShutdown', …)` at `src/__tests__/DailyPlanner.weekNavigation.test.jsx:11`. `package.json` untouched (FullCalendar stays declared). (card `t_aa4715eb`)
- [x] 0.3 Execute the `DailyShutdown` decision as recorded in `design.md` §4 (D8): **deleted, not revived**, with no change to `daily_notes` data or the `/api/daily-notes` route; audit F42 stays an open item. (card `t_aa4715eb`)
- [x] 0.4 Verify stage 0: dead code has no importer, the pre-change and post-change scoped baselines match, the fence holds. (card `t_90607018`, `df-tester`) **PASS 2026-09-25 (PR #22, verified head `f29e1823`, base `design/ui-modernization` @ `5bdf95aa`):** two clean worktrees (base + branch) both give 27 files / 326 tests / 1 failed, with a JSON-reporter inventory diff showing **0 tests moved** (only `MiniWeekBar.test.jsx:39`, pre-existing); the four components and their stylesheets have **0 module references** left and their **1,828** CSS lines are confirmed by blob `wc -l` (916/486/353/73); `npx eslint src` **8 -> 7** (the delta is the deleted `Calendar.jsx:102`); `npx vite build` is **byte-identical**, proving the deleted CSS was never bundled; the fence held (nothing under `server/`, `data/`, `src/api.js`, `src/utils/*`; `package.json` unchanged); the tester booted the branch itself (own API on a `.backup` copy + Vite on scratch ports) and all six views render with a clean console; the live DB was unchanged at 387/6/408/4 with `integrity_check ok`. Two non-blocking nits recorded: `design.md` §10 called the lint row "identical before and after" (it was 8 -> 7, corrected at close-out) and two stale `DayPanel` comments remain (`src/App.jsx:223` plus the historical `.opencode/plans/*` documents). The branch has since gained `711ba5fd` (a merge of `master` into it) - the verified artefact is `f29e1823`.

## 1. Token layer

- [x] 1.1 Add `src/styles/tokens.css` with every name frozen in `design.md` §2 (D2), the ADR-012 palette, the measured derived tones (D4), **both** mode sets (D5), the type/space/radius/motion scales and one `prefers-reduced-motion` block; import it first in `src/main.jsx`. (card `t_65d60fc8`, `df-fullstack`, branch `feat/ui-s1-tokens`) **Done 2026-09-25:** `src/styles/tokens.css` is 110 lines / 63 custom-property declarations (50 in `:root`, 13 in the D5 dark set), every D2 name present — 17 colour, the 8-row type ladder + 4 weight and 2 leading companions, 8 space, `--radius-control|surface|pill` + `--border` + `--shadow-overlay`, `--motion-fast|state` + `--ease` + the two focus-ring measures + `--font-stack`. The dark set is live and inert (adding `.dark` to `:root` re-reads `--page` as `#101418`, measured in the browser). `tokens.css` declares custom properties only, per D1 — the one `prefers-reduced-motion` block therefore lives in `src/index.css`, which D6 names as its owner (`design.md` §2 D6 supersedes the looser placement in this bullet).
- [x] 1.2 Rewrite `src/index.css` as the single owner of globals and strip the duplicated globals from `src/App.css`; delete the Google-Fonts `@import` at `src/index.css:1` and use `var(--font-stack)`. Components adopt tokens in their own stage — no component repaint here. (card `t_65d60fc8`) **Done 2026-09-25:** `grep -c "@import url" src/index.css` = 0; `:root` takes font/ground/colour from `--font-stack`/`--page`/`--text-body`, adds the one `:focus-visible` recipe and the one `prefers-reduced-motion` block; `src/App.css` lost its duplicate `body`, `box-sizing` and repeated font-stack rules (335 → 324 lines) and keeps shell classes only, with the shell's grounds/lines/labels moved behind `--page`/`--surface`/`--surface-2`/`--hairline`/`--text-subtle`. **One component file was touched:** `src/components/DailyWorkflow.css` — its two `1px solid var(--border, #E2E8F0)` sites used `--border` as a *colour*, while D2 freezes it as the composite `1px solid var(--hairline)`; declaring the frozen shape made both declarations invalid at computed-value time and blanked the panel border (measured `borderTop: 0px none`), so both now read `border: var(--border)` / `border-bottom: var(--border)` (measured `1px solid #E7E5DF`). No other component stylesheet was edited, and no component was repainted.
- [x] 1.3 Verify stage 1: token names complete, both mode sets present, contrast re-measured against the three light grounds, no font request, substrate-only visual diff. (card `t_f7c31013`, `df-tester`) **PASS 2026-09-25 (PR #23, head `19ddec65`, base `f29e182`):** all **44** D2 token names present, both mode sets declared (dark inert, no toggle), all **18** light WCAG cells reproduce D4 with delta **0.00** (dark within 0.05 of D5), **0 font requests** live (the base fetched Google-Fonts CSS + woff2), import order correct and the duplicate globals gone, fence empty and `package.json` unchanged, plus a full computed-style census (identical element counts, 0 content changes, 0 new overflow) proving the restyle is substrate-only. Gates: branch **27 files / 326 tests / 2 failed == base 27/326/2** (0 tests moved; both failures pre-existing and reproduced at base under the same 00:33 HK clock), `npx eslint src` **7 == 7**, `npx vite build` ok (JS identical, CSS +1,702 B). One deviation ruled and recorded (`DailyWorkflow.css` +6/-2, the necessary fix to D2's frozen `--border` composite) and one non-blocking finding flagged forward to stage 2 (the token focus ring does not render on the shell's nav/`.btn` controls).

## 2. Shell + cold open

- [x] 2.1 Rail → ~52px top strip: `Sidebar.jsx`/`.css` → `TopStrip.jsx`/`.css`, four text destinations + a Radix "More" overflow (Finance · Projects · Backup · Notifications); every destination reachable at ≤768px (closes F16). (card `t_5d2bcf9d`, `df-fullstack`, branch `feat/ui-s2-shell`) **Done 2026-09-25:** `git mv` kept the history; the strip measures **54px at 1440 and 768** (82px at 420, where it wraps to two rows) with **0 hidden and 0 horizontally clipped** destinations, and all six views were clicked through at **1440 / 768 / 420** in the card's own headless Chrome against the worktree's own API + a `sqlite3 ".backup"` copy of the DB — the old `display: none` dead end (`Sidebar.css:93-98`) is gone. `@radix-ui/react-dropdown-menu` is the only dependency added; Escape closes the menu; 0 console errors.
- [x] 2.2 Today is the cold open (`App.jsx:41`) and one `CaptureLine.jsx` in the shell is present on every view, focused by `/`, inheriting the last-used project/priority from `localStorage['dayframe.captureDefaults']` (D9). (card `t_5d2bcf9d`) **Done 2026-09-25:** `useState('today')`; the line is mounted once by `App.jsx` in `.app-chrome` and `TodayView.jsx` no longer renders one (a unit test asserts its absence there). Live run in the card's headless Chrome on the **Review** view: `/` focused the line and typed nothing (`value === ""`), Enter created task **414** with `dueDate 2026-09-25` (today, HK, computed at run time), `status pending`, no project, `priority medium`, and the row appeared in Today's list with no reload. Choosing project 1 / priority `high` wrote `{"projectId":1,"priority":"high"}` to `localStorage['dayframe.captureDefaults']` (read back) and the next capture created task **415** with `projectId 1, priority high` — both verified by re-reading `/api/tasks`, not assumed.
- [x] 2.3 Verify stage 2: 1440/768/420 reachability, cold open, capture contract, `/` key both ways, capture defaults read back, no router. (card `t_227baf3f`, `df-tester`) **PASS 2026-09-26 (PR #24, head `9125d52`, base `19ddec65`):** strip **54px / 82px** with **0 hidden / 0 clipped** destinations at 1440 / 768 / 420, all six views reachable at both narrow widths, cold open = Today, one capture line per view with copy-DB read-back (task 415 medium/null, 416-417 project 1/high, 418 fallback null/medium, all `dueDate 2026-09-26`, pending), the `/` key both directions, D9 defaults read back from `localStorage`, keyboard tab order + More open/Escape, and Backup downloads a 758,243 B JSON. Gates: **27 files / 341 tests / 340 passed / 1 failed** (pre-existing `MiniWeekBar`) vs base 27/326/325/1 with 0 status changes; `npx eslint src` **7 == 7**; `npx vite build` ok. Fence clean; live `data/app.db` unchanged (388/6/408/4, 0 QA rows).

## 3. Today + habits

- [x] 3.1 Rows instead of cards on Today, the Week day columns and the backlog; row actions reachable in focus and on touch, never hover-only (F10); keep the touch arrow buttons. (card `t_7fd24929`, `df-fullstack`, branch `feat/ui-s3-today-habits`) **Done 2026-09-26:** hairline rows with no card fill / colour stripe / shadow (computed `border-bottom: 1px solid rgb(231,229,223)`, `background: transparent`, `border-left-width: 0px`, `box-shadow: none`, title 15px/500, meta 12px) on Today, all 12 Week-view day-column rows and the backlog; a row is focusable (`tabIndex=0`) and its actions measure `opacity: 1` when focused and under touch emulation (`hover: none` + `pointer: coarse`), `0` at rest with a pointer; the up/down arrows stay as the touch reorder controls at label size.
- [x] 3.2 One-click habit logging with zero minutes through the existing entry path (never a second POST for the same day — ADR-008's 409), duration as an optional in-place refinement, the shared minutes input removed (F22/F23), heat map and streaks kept; Radix popovers replace the hand-rolled ones. (card `t_7fd24929`) **Done 2026-09-26:** one activation wrote exactly one row — `habit_entries(id 463, habitId 2, date 2026-09-26, timeSpentSeconds 0)`, no second row and no 409 in the network log; a second activation deleted it (0 rows); the refinement PUT the same row (`id 462 → timeSpentSeconds 900`); each row of the Habits view owns its own value field (`Read 30 / Meditate ''` after typing into one row). `TimePopover` / `RepsPopover` / `DeferPopover` / the heat-map popover now render through `RadixPopover.jsx` (Radix popover), icon-only buttons through `TooltipButton.jsx` (Radix tooltip).
- [x] 3.3 Verify stage 3: one-click write checked in the DB (one row, `timeSpentSeconds = 0`, no 409), per-row isolation, keyboard/touch reachability, undo, `todayOrder`/`sortOrder` and recurrence integrity. (card `t_02d0e623`, `df-tester`) **PASS 2026-09-26 (PR #25, head `7f186df1bfe1c125591984b7be8d57fa48e1e183`, base `feat/ui-s2-shell`):** all ten acceptance items hold - hairline rows with transparent / no-stripe / no-shadow computed styles on Today, the Week day columns and the backlog (1440 + 768 shots); row actions reachable by keyboard (Tab + Enter activated a reorder) **and** with hover unavailable (touch emulation - CDP `hover:none` media emulation is a no-op on this box); one activation writes exactly one 0-minute habit entry (no 409, no duplicate), a second unmarks it, the refinement PUTs the same row; per-row value fields are independent (F23); duration-first logging makes a single POST; complete / defer / reorder each offer a 5-second undo the DB confirms; drag + arrow reorder move `settings.todayOrder` and each task's `sortOrder` together and the Week today column agrees; completing a recurring instance writes `statusOverrides` on the base task and `recurrencePattern` parses as an object. Gates: **28 files / 350 tests / 1 pre-existing failure** vs base 27/341/1; `npx eslint src` **6 vs 7**; `npx vite build` ok; `openspec validate` valid. Live DB untouched (388 tasks / 6 habits / 408 entries, 0 entries that day, `integrity_check ok`). One declared gap: 12 decoration-only `:hover` rules still lack a focus twin (no action is hover-gated; handed to stage 5).

## 4. Week / review / finance

- [x] 4.1 Repaint the Week, Review, Finance, Projects and modal surfaces onto tokens; delete their literal hexes/sizes/radii (Projects' Tailwind-gray ramp is F04). (card `t_63c2607b`, `df-fullstack`, branch `feat/ui-s4-week-review-finance`) **Done 2026-09-26:** `MiniWeekBar.css`, `WeeklyReview.css`, `MonthlyReview.css`, `ProjectsView.css`, `WeeklyObjectives.css`, `TaskModal.css`, `HabitModal.css`, `ProjectModal.css` and `RichTextEditor.css` now carry **0** literal hexes, **0** literal `font-size` and **0** literal `border-radius` (grep per file), with the 49 literal `font-weight`s moved onto the weight tokens, the last `rem` measures onto the space scale (ProjectsView and TaskModal previously had none left — F04's `rem` outlier is gone) and every `1.5px` border replaced by `var(--border)`. The 10 `outline: none` sites in these files were deleted so the one global `:focus-visible` recipe applies (design.md §12 allocates the outline sites to the stage that repaints each component); the app-wide `outline: none` occurrence count drops **17 → 4** (16 real rules → 3; the 3 that remain are `DailyWorkflow.css` and `YearlyGoals.css`, which stage 4 did not repaint, plus the prose mention inside `index.css`'s own comment).
- [x] 4.2 Make the two weeks unmistakable (F35): the planning week and the reviewed week (`weekStart − 1`, ADR-005) each carry an explicit date label; the habit panel keeps reading W−1. (card `t_63c2607b`) **Done 2026-09-26:** the week nav renders two labelled windows — in the browser with the selected week at W = Sep 21–27 the labels read `Planning week · Sep 21 – Sep 27, 2026` and `Reviewed week · Sep 14 – Sep 20, 2026`, the habit panel is titled `Habits — reviewed week Sep 14 – Sep 20, 2026`, the key-events grid `Key events — planning week Sep 21 – Sep 27, 2026`, and the old `(Review week: …)` parenthetical is gone (a test asserts `/Review week:/` is absent). The window itself did not move: the network log shows the habit fetch going out as `?from=2026-09-14&to=2026-09-20` (= W−1) while the document stayed on `?weekStart=2026-09-21`.
- [x] 4.3 Debounced saves with one visible status line (D12) in the review, the objectives panel and the finance review; Radix dialogs replace every `window.confirm/alert/prompt` (0 call sites under `src/`). (card `t_63c2607b`) **Done 2026-09-26:** `src/components/useDebouncedSave.js` (500 ms, flush on blur / period change / unmount) + `SaveStatus.jsx` are used by `WeeklyReview`, `MonthlyReview` and `WeeklyObjectives`. Measured in headless Chrome: typing 28 characters into a reflection field produced **0** writes while typing and **1** `PUT /api/weekly-reviews` after the pause; the status line read `Saved` (`role="status" aria-live="polite"`), and with the request blocked it read `Could not save — retry` with `data-status="error"`. `src/components/AppDialog.jsx` (+ its `ConfirmDialog` / `PromptDialog`) is the one dialog shell and owns the focus trap, Escape and focus return; `grep -rn "window\.\(confirm\|alert\|prompt\)" src/` → **0**, and `TaskModal` / `HabitModal` / `ProjectModal` render their shells through it (which is what gives them Escape dismissal). Verified: the objectives-replacement confirm and the Projects delete confirm both close on Escape with **0** writes afterwards, and focus returned to the invoking control (`Delete project General`).
- [x] 4.4 Wire the two dead day-select controls (F20) with view-local state, and reconcile the stale `MiniWeekBar.test.jsx` expectation with the component's real control set. (card `t_63c2607b`) **Done 2026-09-26:** `DailyPlanner.handleSelectDay` is now passed to both the week bar (`onSelectDate`) and every `DayColumn` (`onDayClick`), whose header is a `<button>` again; it sets a view-local `focusedDay` used for the column emphasis and a `scrollIntoView`. Measured: clicking the week bar's `Thu 24` gave exactly one `.dc-column--focused[data-date="2026-09-24"]` with `aria-pressed="true"` on that day button and the matching header button; clicking the 7th column's own header moved the focus to `2026-09-27` and the week bar's selected day followed (`Sun 27`). No app-level state, no route. `MiniWeekBar.test.jsx` now asserts the component's real control set — 7 day buttons + `‹` prev + `Today` + `Next ›` = **10** buttons (the old expectation of 9 counted only the two arrows and had been failing since the bar gained Today) — and the suite is fully green.
- [x] 4.5 Verify stage 4: both weeks visible and W−1 proven, debounce/status line, dialogs with focus return + Escape, finance read-only month and ≤8 MB paste, suite fully green. (card `t_472d4fde`, `df-tester`) **PASS 2026-09-26 (PR #27, verified head `fd468c9`, base `7f186df`):** all 9 acceptance items hold - both week labels named with their own dates while the habit panel still fetched W-1 (`from=2026-09-07&to=2026-09-13` at W=2026-09-14); **68 keystrokes -> 0 writes -> 1 PUT**, a blocked write showing `Could not save - retry`, and blur flushing a pending write; all four dialogs (task delete, project delete, objectives replacement, link prompt) trapping focus, closing on Escape with **0 writes** and returning focus to the exact invoking control, with **0** `window.confirm/alert/prompt` call sites; the finance month still read-only with thumbnails + lightbox and the **<=8 MB** paste still refused; the objectives bridge writing + warning + persisting the sync flag per week; both day-select controls now select a day; **0** literal hex / font-size / radius in the 9 repainted stylesheets. Gates: **29 files / 361 tests / 0 failed vs base 28/350/1** (0 new failures); `npx eslint src` **6 == 6**; `npx vite build` ok; `openspec validate` valid. Live DB untouched (388/6/408/4/12/16, `integrity_check ok`, 0 QA rows). One non-blocking deviation filed as `t_6f8491c4` (the status line's horizontal slot was not reserved, so the adjacent control shifted 31-123 px); that branch has since gained the fix (`9a63ffc`, `f72fa1a9`, `src/components/SaveStatus.css` only) **after** `fd468c9`, so it is **not** in this close-out branch's ancestry.

## 5. Keyboard layer + palette

- [x] 5.1 Visible focus everywhere (the token focus recipe as `:focus-visible`; no `outline: none` without a replacement indicator) and the frozen key map from `design.md` §6 (D10): `/`, `j`/`k`, `x`, `t`, `u`, `g`+view, `Escape`, `Ctrl/⌘+K`. (card `t_4925715f`, `df-fullstack`, branch `feat/ui-s5-keyboard`) **Done 2026-09-26:** `grep -rn 'outline:\s*none' src --include=*.css` = **0** (the three survivors — `DailyWorkflow.css`'s add-step field, `YearlyGoals.css`'s textarea and add field — lost the declaration; `src/index.css` keeps the one global `:focus-visible` recipe on `--focus-ring-width`/`--focus-ring`/`--focus-ring-offset`). The map lives in the new `src/components/keyboard.js` (frozen `GOTO_VIEWS`, `SHORTCUTS`, `isTypingContext`, `isOverlayOpen`, `moveRowFocus`, `handleRowKeyDown`, `tomorrowDateStr`) with one registration point per key: `/` in `CaptureLine.jsx` (stage 2 — it needs that input's ref), `j`/`k` + the `g` chord + `Ctrl/⌘+K` in `useKeyboardLayer.js` (mounted once by `App.jsx`), `x`/`t` in each row's own `onKeyDown`, `u` in `UndoToast.jsx`, `Escape` in each overlay's Radix dismissable layer. Every list row is its own focus target (`tabIndex={0}` + `data-kbd-row`, list containers carry `data-kbd-list`) and the app's one pre-existing `tabIndex` (`MonthlyReview.jsx`'s photo paste zone) is untouched.
- [x] 5.2 List navigation + the 5-second undo from stage 3, and the command palette as the last, **droppable** slice (no new router, no new `activeView` key, no new app-level state). (card `t_4925715f`) **Done 2026-09-26:** `j`/`k` move focus between `[data-kbd-row]` elements inside the focused row's `[data-kbd-list]` (Today's task panel, each Week day column's body, the backlog), or the first list on the screen when nothing is focused; `x`/`t` call the surface's **existing** handlers, so the keyboard drives the same core state as the pointer (`todayOrder`/`sortOrder`, the recurring status override, `dueDate`). `u` is owned by the stage-3 `UndoToast` — one 5-second mechanism, no second stack. The palette is `CommandPalette.jsx`/`.css` + one mount in `App.jsx` + the `Ctrl/⌘+K` entry, and it is droppable: removing those three leaves the suite green (proved by removal, see the card comment). No router, no seventh `activeView` value; the only app-level state added is `App.jsx`'s `paletteOpen` boolean.
- [x] 5.3 Verify stage 5: the full key map exercised, the not-while-typing rule, focus visibility, palette droppability proven by removal, core-state integrity. (card `t_2193556a`, `df-tester`) **PASS 2026-09-26 (PR #28, head `6c120ab`, base `fd468c9`; criterion-by-criterion table in `attachments/t_2193556a/EVIDENCE.md`, with the head SHA and the three gate numbers inside):** 0 `outline:none` / 26 `:focus-visible` rules with a Tab walk of **198/200** stops visibly indicated (the 2 exceptions are one `input[type=date]` internal sub-field stop where `:focus` and `:focus-visible` are both false - reproduced on a bare injected control **and** at base `fd468c9`, so it is UA-level, not an app rule); every frozen binding exercised with real key events (`/ j k x t u g`+6 views `Escape Ctrl+K`) with no missing and no extra binding; `jkxt` typed literally into the capture line, a modal field, the ProseMirror editor and the palette search with Tab never consumed and a clean console; the palette opens / lists 7 commands / navigates / closes on Escape returning focus to the invoking row, and its droppability is proven **by removal** (8 failed / 375 passed = exactly the palette cases, the app still works, then restored byte-identical); a keyboard reorder moved `settings.todayOrder` and both `sortOrder`s together (read-only DB copy) and a recurring instance `x` wrote the instance-keyed `statusOverrides` on base task 323; fence clean (no `server/`, DB, `src/api.js`, `src/utils/*`, no router, 6 `activeView` comparisons, only `paletteOpen` added); gates **vitest 30 files / 383 tests / 0 failed vs base 29/361/0**, `npx eslint src` **6 == base 6**, `npx vite build` ok. Live `data/app.db` untouched (388/1/408/6/4, `todayOrder` unchanged, `integrity_check ok`, 0 QA rows). The tester left this bullet unticked on purpose - the tester does not push to the branch under test and does not self-certify its own gate; this card ticks it with that reference.

## 6. Close-out

- [x] 6.1 Tick the stage bullets with their evidence, `openspec validate … --strict`, then `openspec archive ui-modernization-calm-canvas` and check the merged deltas landed in `openspec/specs/`. (card `t_55c01442`, `df-lead`, branch `docs/ui-modernization-archive` from `feat/ui-s5-keyboard`) **Done 2026-09-26:** all six verification bullets above are ticked with the PR number, head SHA and the measured numbers each card was accepted on; 5.3 was left unticked by the tester on purpose (the tester does not push to the branch under test and does not self-certify its own gate). `openspec validate ui-modernization-calm-canvas --strict` -> **valid** (14 deltas). `openspec archive -y ui-modernization-calm-canvas` -> **`openspec/changes/archive/2026-09-26-ui-modernization-calm-canvas/`**, totals **+9 created / ~5 modified / -0**: nine requirements created in the new `ui-presentation-system`, and five modified across `today-item-ordering` (2), `today-quick-capture` (1) and `weekly-review-planning` (2). Merged-result checks: the new text landed (the both-weeks rule and the reviewed-week window in `weekly-review-planning`, the capture line present on every view in `today-quick-capture`, the focus/touch-reachable reorder controls in `today-item-ordering`) and **no `## MODIFIED Requirements` header leaked into any of the three base specs**. Two archive attempts aborted first: all three targets were legacy delta-shaped base specs (a delta header instead of `## Requirements`, and no `## Purpose`) - exactly the leak this bullet told us to look for, inherited from earlier archives. Repaired on this branch (delta header -> `## Requirements`, plus `# <name> Specification` + a `## Purpose`; **no requirement text touched**). **Six other base specs remain delta-shaped** and are recorded in `DECISION_LOG.md` §D.
- [x] 6.2 Rule B final sweep (`ARCHITECTURE.md`, `ROUTE_MAP.md`, `DECISION_LOG.md` open items), final green suite/lint/build, and the fan-out reported back on the proposal card. (card `t_55c01442`) **Done 2026-09-26:** Rule B final sweep on `docs/ui-modernization-archive`, all three documents updated in this branch. `ARCHITECTURE.md` v1.7: §1/§2/§3/§4 re-checked against the code as shipped (the top strip, the one capture line, the token layer, the Radix-owned dialogs/popovers, the keyboard layer + droppable palette, the four deleted components) and every UI-direction citation retargeted from ADR-012 to **ADR-013** (renumbered on `master` on 2026-09-26 when the habits ADR claimed ADR-012); §7's "the single failure is the pre-existing `MiniWeekBar`" note corrected - stage 4 reconciled that test and the suite is green. `ROUTE_MAP.md` v1.6: the same retargeting, plus the `/api/habits` row merged from `master` (the `frequency` normalization contract, which legitimately cites ADR-012) and the header re-based on `master`'s v1.3. `DECISION_LOG.md` v1.4: re-based onto `master`'s v1.3 and extended - the UI-modernization entry now reads *shipped and archived* (with the merge-order caveat), the `MiniWeekBar` and orphan-component items are struck as resolved, and FullCalendar removal, the F42 ritual, the D6 token residue (`t_e904b822` / `t_e518956a`), the two accepted cosmetic focus residuals and the six delta-shaped base specs are open items. No `.dev_context` document describes the 220px rail or the four dead components as live: the only remaining `220px` hits are ROUTE_MAP's past-tense "replaced the 220px rail" and three `src/` widths that are not the rail (`DailyTimeline.css`, `DeferPopover.css`, `MonthlyReview.css`). Gates on this branch: `npx vitest run --exclude='**/.worktrees/**'` **30 files / 383 tests / 0 failed**; `npx eslint src` **6 problems (6 errors)** == base 6; `node node_modules/vite/bin/vite.js build` **ok** (523 modules, 295 ms); `openspec list` no longer lists the change as active. The fan-out is reported on the proposal card `t_f0914f40`.

## Rule B — per stage, not at the end

| Stage | Document to update in the same branch |
|---|---|
| 0 | `ARCHITECTURE.md` §2/§7, `ROUTE_MAP.md` §1 — the four components are deleted |
| 1 | `ARCHITECTURE.md` §1 — the token layer as the styling substrate (cite ADR-012, do not restate the palette) |
| 2 | `ARCHITECTURE.md` §2/§3/§4.1, `ROUTE_MAP.md` §1 — the shell, the capture line, the six destinations |
| 3 | `ARCHITECTURE.md` §2/§4.1/§4.2, `ROUTE_MAP.md` §1 — rows, focus actions, one-click habits, Radix popovers |
| 4 | `ARCHITECTURE.md` §2/§3/§4.3/§4.4, `ROUTE_MAP.md` §1/§3 — dialogs, both week labels, the focused day |
| 5 | `ARCHITECTURE.md` (keyboard sub-section), `ROUTE_MAP.md` §1 — the palette |
| 6 | Final sweep + `DECISION_LOG.md` open items |

## Out of scope (do not start these here)

| Item | Why it is not in this change |
|---|---|
| Concept B — Timeline Studio | Deferred behind a **time-model ADR** reconciling `scheduledTime` / `startTime`+`endTime` / `estimatedMinutes` (ADR-012). Its fields must not leak into any stage. |
| Tailwind / shadcn-ui / Framer Motion | Deferred with recorded re-entry triggers (ADR-012). |
| Dark mode | Designed for (both token sets written), not built. |
| Removing the FullCalendar dependency | Its only consumer is deleted in stage 0, but dependency removal is its own decision with lockfile/build risk. New open item. |
| The F42 daily close-out ritual | `DailyShutdown.jsx` is deleted in stage 0; a proper close-out surface belongs to a later change once Today's rows have landed. New open item. |
| Habit **count** tracking (ADR-011) | Needs two `ALTER TABLE`s plus route plumbing, i.e. server + DB work — explicitly outside a presentation-only change. |
| The ~115 dead class names inside live stylesheets (F12) and the `assignedTo` UI gap | Those stylesheets are repainted in stages 3–5; the dead rules are deleted as part of that repaint, not as a separate sweep. |
| A router, or a new `activeView` key | The six views and one state string stay; ADR-012 forbids a framework migration. |
| First-run/empty-state design (F40), print/export (F41), `.dev_context` on `master` | Recorded audit findings and process items outside this change's acceptance. |
